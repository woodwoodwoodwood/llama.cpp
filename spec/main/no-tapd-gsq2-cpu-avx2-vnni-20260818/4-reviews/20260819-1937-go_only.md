# no-tapd-gsq2-cpu-avx2-vnni-20260818 — Review 报告（fix 确认轮）

> Source: conversation  |  Subdomain: main  |  Complexity: M
> Date: 2026-08-19 19:37

## 0. Summary Card【必读】
- review_mode: go_only（**fix 确认评审**：上一轮 go_only 的待修复清单 4 项已全部标 resolved，按 failure-modes F6 触发的确认轮）
- model: codebuddy-glm-5.3（⚠️ 协议偏离：`meta.yaml.models.review=opus`，但本轮执行环境为 CodeBuddy/GLM-5.3，无法切换会话模型。按 SKILL「切换失败应停止」的条款本应中止；因用户显式要求在本会话执行，以显著标注偏离的方式继续。如需严格合规，请在 Cursor 中以 opus 复跑本轮）
- ⚠️ 未结合验证结果，findings 可能因后续验证结果而调整/失效
- Findings：P0=0 / P1=0 / P2=0
- Spec 偏离：minor=0 / major=0
- **Fix 确认结论：FIX-1 ~ FIX-4 全部在提交态代码中核实通过**

## A. 总览【必填】
- Scope：确认对象为 `d4bf44327`（HEAD）vs `325fa87b9`，8 文件 / +631 -2。上一轮评审 scope 为 `7b296cf4a` vs `325fa87b9`（7 文件 / +444 -2）；fix 已 amend 进同一提交，hash 经 `7b296cf4a → 9faf2bdd7 → d4bf44327` 演进
- 覆盖维度：四项 FIX 逐条代码核实（阈值 / from_float 一致性 / backend 检测 / unaligned load）、§D 约束清单在提交态的复核、L1/L1′/L2 spec 检查、handoff 一致性
- 复核方式：测试文件（`tests/test-gsq2-vec-dot.cpp`、`tests/test-quantize-fns.cpp`、`tests/CMakeLists.txt`、`arch-fallback.h`）工作区未修改 → 与 HEAD 一致，直接读工作区；`arch/x86/quants.c` 工作区已被后续未提交改动污染 → 用 `git show HEAD:` 取提交态核实

## B. P0 必改项
（无）

## C. P1 应改项
（无）

## D. P2 建议项
（无——本轮确认范围限于提交态代码，与提交无关的工作区/流程观察项不在此列）

## E. 需求完整度【必填】
- `1-change-plan.md §B` 改动点覆盖：5/5 已实现（核 / x86 alias 删除 / test-gsq2-vec-dot / CMakeLists 注册 / quantize-fns 阈值），与上一轮结论一致，fix 未破坏任何 §B 条目
- `0-clarification.md §E/§F`：`work_type=perf` 占位类型（仅 §A/§B），沿用上一轮判定（R-QUAL-07 warning，不阻塞）
- `0-context-snapshot.md §D` 约束清单核对（针对**提交态** d4bf44327）：
  - 核 diff 限于 `arch/x86/quants.c` 与 `arch-fallback.h` x86 分支 — 遵守（提交 diff 中代码文件仅此二者 + 声明的测试三文件）
  - 未改 `block_gsq2` / quantize / dequant / CUDA / traits — 遵守
  - 非 AVX2 回退 `ggml_vec_dot_gsq2_q8_0_generic` — 遵守
  - 非 x86 fallback alias 未删 — 遵守（arch-fallback.h diff 仅 -1 行，为 x86 行）
  - `quants.h` 签名未改 — 遵守（提交态；工作区改动不属本需求）
  - 测试三文件范围 — 遵守

## SC. Spec Check Report

## Spec Check Report
- spec: 0-clarification.md ; 1-change-plan.md
- spec_type: change | change-plan
- level_run: L1+L2
- result: PASS_WITH_WARNINGS
- summary: 四项 fix 全部核实落地且未引入新的 spec 偏离；两处占位 warning 沿用上轮

### Errors
（无）

### Warnings
- [R-QUAL-07] `perf` 澄清仍为 §A/§B 占位（沿用上轮；细节在 change-plan）
- [R-PLAN-03] 澄清无 `suggested_tests`；plan §E 已自补 6 条可验证测试点（沿用上轮判定）

### Passed
- [R-STRUCT-01/02/03] perf 类型仅要求 §A/§B；frontmatter 齐全；work_type 与 meta 一致
- [R-PLAN-01/02/04/05] §A~§G+Env 必填节齐全、条目含路径/函数名、Env 回答自检与容器问题、§A 有选型理由（复用 `mul_sum_i8_pairs_float`）
- [R-DIFF-01/03] 提交 diff 全部路径可在 plan §B 找到声明；无文档过宽/漏改

## SR. Spec Reconciliation
本次无 spec 偏离（上轮 SR-1 已于 2026-08-19 reconcile，本轮核对 `0-clarification.md:14`「按『核 2 文件 + 对照单测 / 单仓单人』定为 M」已生效）。

## FIX 确认明细【本轮核心】
- [x] FIX-1 [P1] `tests/test-quantize-fns.cpp` GSQ2 独立阈值 — 状态: **resolved 确认**
  提交态证据：`MAX_QUANTIZATION_TOTAL_ERROR_GSQ2 = 0.010f`（L26，附成因注释：`d=amax/2` 非对称 codebook，实测 ~0.00796）；`MAX_DOT_PRODUCT_ERROR_GSQ2 = 0.25f`（L32）；分派点 L168 / L188-189 均走 GSQ2 档。未改 `quantize_row_gsq2_ref` ✓
- [x] FIX-2 [P2] `test-gsq2-vec-dot.cpp` 对照 Q8 与 mul_mat 同一 `from_float` — 状态: **resolved 确认**
  提交态证据：`quantize_q8_0_cpu()`（L82-126）经 CPU backend `ggml_cpy(F32→Q8_0)` 图量化，L81 注释明示「Same Q8_0 path mul_mat uses (CPU from_float: x86 round-to-even)」；`test_mul_mat_matches_generic`（L283）与 `test_pack_e4_mul_mat`（L340）均已改用该路径。现场复核：本会话实测 `test-gsq2-vec-dot.exe` 5 用例全 ok
- [x] FIX-3 [P2] `test-gsq2-vec-dot.cpp` 记录/要求 AVX2 backend — 状态: **resolved 确认**
  提交态证据：`cpu_backend_has_avx2()`（L48-79）读 loaded backend features 并拼 description；main 打印 `CPU backend: ...`（L379）；无 AVX2 时 mul_mat 核对照显式 SKIPPED 并注明「generic vs generic」（L383-384），不再假绿。现场复核：实测输出 `CPU backend: CPU / Intel(R) Core(TM) Ultra 7 265KF AVX2=1`
- [x] FIX-4 [P2] `arch/x86/quants.c:102` 显式 unaligned load — 状态: **resolved 确认**
  提交态证据（`git show HEAD:ggml/src/ggml-cpu/arch/x86/quants.c`）：`bytes_from_2bit_32` 用 `uint64_t packed; memcpy(&packed, rsi, sizeof(packed)); _mm_cvtsi64_si128(...)`，注释「Avoid _mm_loadl_epi64」；该 helper 无 `_mm_loadl_epi64` 残留。与同文件其它 helper 的 memcpy 风格一致 ✓
  - 注：工作区未提交的 wide 内核改动已整体删除该函数（被 ~8 指令 wide 解包取代），属下一需求的范围，不影响本轮对提交态的确认

## 待修复清单【同 go_only 模式内迭代，见 F6】
- [x] FIX-1 [P1] — 状态: resolved（by d4bf44327，2026-08-19 确认）
- [x] FIX-2 [P2] — 状态: resolved（by d4bf44327，2026-08-19 确认）
- [x] FIX-3 [P2] — 状态: resolved（by d4bf44327，2026-08-19 确认）
- [x] FIX-4 [P2] — 状态: resolved（by d4bf44327，2026-08-19 确认）
