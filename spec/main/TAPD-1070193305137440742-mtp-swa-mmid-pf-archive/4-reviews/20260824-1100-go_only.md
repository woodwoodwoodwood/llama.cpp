# TAPD-1070193305137440742-mtp-swa-mmid-pf-archive — Review 报告

> Source: TAPD #1070193305137440742（拉取失败已转手动贴单，`fetch_status=degraded`）  |  Subdomain: main  |  Complexity: M
> Date: 2026-08-24 11:00

## 0. Summary Card【必读】
- review_mode: go_only
- model: opus（`meta.yaml.models.review`）
- ⚠️ 未结合验证结果，findings 可能因后续验证结果而调整/失效（无 `3-verify-report.md`，尚未跑 compile/unit）
- Findings：P0=0 / P1=0 / P2=1
- Spec 偏离：minor=0 / major=0（无偏离）

## A. 总览【必填】
- Scope：2 文件 / +10 -0 行（`tools/server/server-context.cpp` +6，`tests/CMakeLists.txt` +4）
- 覆盖维度：代码质量/可读性、边界值健壮性、L1（clarification 结构）、L1′（change-plan 结构）、L2（spec↔diff 一致性）、`0-context-snapshot.md §D` 约束清单核对

## B. P0 必改项
（无）

## C. P1 应改项
（无）

## D. P2 建议项【可折叠】
- `tools/server/server-context.cpp:1270` — `uint32_t w = (uint32_t) std::atoi(e);` 对负数/非数字输入的健壮性是**预置行为**（改动前就存在），本次新增的护栏未覆盖这一路径：若 `LLAMA_MTP_SWA` 设为负数（如 `"-1"`），`std::atoi` 返回 `-1`，转 `uint32_t` 后变成 `4294967295`，`nub > w` 几乎必为 false（新护栏形同虚设），随后 `cap = 2u * w * np` 会无符号环绕（wrap）成一个不可预测的小值，`cparams_mtp.n_ctx` 可能被设成一个荒谬值。这是**预置缺陷**，不在本次 §B 确认范围内（仅确认 `n_ubatch<=W` 检查），本次改动未使其变坏也未修复，仅作为后续可能需要关注的建议项记录，不阻塞本轮。

## E. 需求完整度【必填】
- `1-change-plan.md §B` 改动点覆盖：2/2 已实现（护栏 inline 插入位置、行号与计划描述完全一致；CMake 双 CTest 注册与计划描述完全一致）
- `0-clarification.md §B` 业务规则：第 3 条"补一个运行时 assert/报错，拒绝 `n_ubatch>W` 非法组合"——实现为 `SRV_ERR` + `return false`（非 `assert`，属于同等语义的更合适实现：`assert` 在 release 构建通常被 `NDEBUG` 剥除，而 `SRV_ERR`+`return false` 是文件内既有失败路径风格，等价甚至更稳妥，不视为偏离）
- `0-context-snapshot.md §D` 约束清单核对：
  - "两开关未设置时行为一致（no-op）" → 遵守（护栏在 `if (w > 0)` 内，`w==0` 分支不受影响；MMID_PF 侧代码未改动）
  - "GGML_MMID_PF 纯预取 hint，数值逐位一致" → 遵守（未改生产代码，只加测试注册）
  - "LLAMA_MTP_SWA 只影响接受率不改输出" → 遵守（未改该逻辑本身）
  - "新护栏在 `w==0` 时不触发/无额外开销" → 遵守（护栏代码在 `if (w > 0)` 块内部）
  - "LLAMA_MTP_SWA 护栏为人工验证项，不要求自动化单测" → 遵守（未新增该项自动化测试，符合用户明确决策）
  - "`llama-model.cpp` 里独立解析逻辑本轮不改动" → 遵守（diff 未涉及该文件）
  - 全部遵守

## SC. Spec Check Report【必填】

```markdown
## Spec Check Report
- spec: 0-clarification.md ; 1-change-plan.md
- spec_type: change ; change-plan
- level_run: L1+L2
- result: PASS_WITH_WARNINGS
- summary: 两份文档结构完整、字段可执行；diff 与计划完全对应，无越界改动。

### Errors
（无）

### Warnings
- [R-QUAL-07]（clarification）`work_type=perf` 仅有 §A/§B（占位模板），属类型固有限制，非本次缺陷。
- [R-PLAN-03]（change-plan）clarification 无 `suggested_tests`（perf 占位类型），规则要求 §E 自行补充可验证测试点——§E 已补 2 条具体测试点（1 条自动化 unit + 1 条人工验证），已满足要求。

### Passed
- [R-STRUCT-01/02/03]（clarification）§A/§B 完整，frontmatter 齐全，work_type 与 meta.yaml 一致
- [R-PLAN-01/02/04/05]（change-plan）§A/§B/§C/§E/§F/Env 齐全且可执行，Env 已诚实回答"本轮不做本地验证"
- [R-DIFF-01]（L2）diff 触及的两个文件均能在 `1-change-plan.md §B` 找到对应声明，无未声明改动
- [R-DIFF-03]（L2）无声明影响面与实际 diff 不匹配的情况
```

## SR. Spec Reconciliation【必填；无偏离写"本次无 spec 偏离"】

本次无 spec 偏离（三分流结果：2/2 改动点 MATCH，0 CODE_GAP，0 SPEC_DRIFT）。

## 待修复清单【本项目新增，供 go 迭代回写，仅在同一 review_mode 内迭代】
（无 open 项）
